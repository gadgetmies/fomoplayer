.worktrees/

# Created by .ignore support plugin (hsz.mobi)
### Node template
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# Typescript v1 declaration files
typings/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env

.idea
secrets.*.js
packages/analyser/venv
analyser/.fomo_player_token
analyser/.embedding_sanity.json
analyser/embedding_collisions.jsonl
analyser/venv/
analyser/__pycache__/
pg/
packages/infrastructure/resources/build/
packages/infrastructure/cdk.out/
packages/back/public/static/
packages/front/build/
packages/integration-tests/tests_output/

# macOS
.DS_Store

# Claude Code local state (skills/commands/settings under .claude stay tracked)
.claude/worktrees/
.claude/scheduled_tasks.lock

# Local dev launcher: exports real credentials, must never be committed
start-multi-store-player.sh

# Front-end build output copied into the back-end by the packages/front build
# (`react-scripts build && cp -r build/* ../back/public/`).
# packages/back/public/auth/ is hand-written and stays tracked.
packages/back/public/index.html
packages/back/public/asset-manifest.json
packages/back/public/manifest.json
packages/back/public/favicon.png
packages/back/public/favicon.svg

# Analyser working data: large and machine-generated.
analyser/downloads/
analyser/panako_db/
analyser/panako_db_worker_*
analyser/last_fingerprint_run.json
analyser/last_score_run.json

# Source audio for the local Panako diagnostics (debug_match.py and the
# panako-gated tests in test_parallel_preview_fingerprinting.py). These are
# full commercial tracks, ~25 MB, and CI does not need them: the backend
# regression test reads pre-extracted fingerprints from
# packages/back/test/fixtures/sample-matching/ instead. Obtain them
# out-of-band; the tests that use them skip when they are absent.
analyser/data/

# __pycache__ anywhere under analyser (the top-level rule above only covers
# analyser/__pycache__ itself).
analyser/**/__pycache__/
