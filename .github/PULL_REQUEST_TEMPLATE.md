## Summary

<!-- 1-3 bullet points -->

## Test plan

<!-- Bulleted checklist of TODOs for testing the PR -->

## Sentry

<!--
If this PR fixes additional Sentry issues beyond the one named in the
`sentry:<id>` label, list each extra ID on its own line inside the fenced
block below. The `sentry-resolve.yml` workflow marks every ID listed here
as resolved when the PR merges. Leave the block empty if there are none.
-->

```sentry-also-resolves
```
